<?xml version='1.0' encoding='utf-8'?>
<TEI xmlns="http://www.tei-c.org/ns/1.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xml:space="preserve" xsi:schemaLocation="http://www.tei-c.org/ns/1.0 https://raw.githubusercontent.com/kermitt2/grobid/master/grobid-home/schemas/xsd/Grobid.xsd">
	<teiHeader xml:lang="en">
		<fileDesc>
			<titleStmt>
				<title level="a" type="main">Using the Output Embedding to Improve Language Models</title>
			</titleStmt>
			<publicationStmt>
				<publisher />
				<availability status="unknown"><licence /></availability>
				<date type="published" when="2017-02-21">21 Feb 2017</date>
			</publicationStmt>
			</fileDesc>
		<encodingDesc>
			<appInfo>
				<application version="0.8.0" ident="GROBID" when="2025-12-22T07:54+0000">
					<desc>GROBID - A machine learning software for extracting information from scholarly documents</desc>
					<ref target="https://github.com/kermitt2/grobid" />
				</application>
			</appInfo>
		</encodingDesc>
		<profileDesc>
			<abstract>
<div><p>We study the topmost weight matrix of neural network language models. We show that this matrix constitutes a valid word embedding. When training language models, we recommend tying the input embedding and this output embedding. We analyze the resulting update rules and show that the tied embedding evolves in a more similar way to the output embedding than to the input embedding in the untied model. We also offer a new method of regularizing the output embedding. Our methods lead to a significant reduction in perplexity, as we are able to show on a variety of neural network language models. Finally, we show that weight tying can reduce the size of neural translation models to less than half of their original size without harming their performance.</p></div>
			</abstract>
		</profileDesc>
	</teiHeader>
	<text xml:lang="en">
		<body>
<div><head n="1">Introduction</head><p>In a common family of neural network language models, the current input word is represented as the vector c ∈ IR C and is projected to a dense representation using a word embedding matrix U . Some computation is then performed on the word embedding U ⊤ c, which results in a vector of activations h 2 . A second matrix V then projects h 2 to a vector h 3 containing one score per vocabulary word: h 3 = V h 2 . The vector of scores is then converted to a vector of probability values p, which represents the models' prediction of the next word, using the softmax function.</p><p>For example, in the LSTM-based language models of <ref type="bibr" target="#b33">(Sundermeyer et al., 2012;</ref><ref type="bibr" target="#b35">Zaremba et al., 2014)</ref>, for vocabulary of size C, the one-hot encoding is used to represent the input c and U ∈ IR C×H . An LSTM is then employed, which results in an activation vector h 2 that similarly to U ⊤ c, is also in IR H . In this case, U and V are of exactly the same size.</p><p>We call U the input embedding, and V the output embedding. In both matrices, we expect rows that correspond to similar words to be similar: for the input embedding, we would like the network to react similarly to synonyms, while in the output embedding, we would like the scores of words that are interchangeable to be similar <ref type="bibr" target="#b29">(Mnih and Teh, 2012)</ref>.</p><p>While U and V can both serve as word embeddings, in the literature, only the former serves this role. In this paper, we compare the quality of the input embedding to that of the output embedding, and we show that the latter can be used to improve neural network language models. Our main results are as follows: (i) We show that in the word2vec skip-gram model, the output embedding is only slightly inferior to the input embedding. This is shown using metrics that are commonly used in order to measure embedding quality. (ii) In recurrent neural network based language models, the output embedding outperforms the input embedding. (iii) By tying the two embeddings together, i.e., enforcing U = V , the joint embedding evolves in a more similar way to the output embedding than to the input embedding of the untied model. (iv) Tying the input and output embeddings leads to an improvement in the perplexity of various language models. This is true both when using dropout or when not using it. (v) When not using dropout, we propose adding an additional projection P before V , and apply regularization to P . (vi) Weight tying in neural translation models can reduce their size (number of parameters) to less than half of their original size without harming their performance.</p><p>Neural network language models (NNLMs) assign probabilities to word sequences. Their resurgence was initiated by <ref type="bibr" target="#b3">(Bengio et al., 2003)</ref>. Recurrent neural networks were first used for language modeling in <ref type="bibr" target="#b21">(Mikolov et al., 2010)</ref> and <ref type="bibr" target="#b30">(Pascanu et al., 2013)</ref>.</p><p>The first model that implemented language modeling with LSTMs <ref type="bibr" target="#b15">(Hochreiter and Schmidhuber, 1997</ref>) was <ref type="bibr" target="#b33">(Sundermeyer et al., 2012)</ref>.</p><p>Following that, <ref type="bibr" target="#b35">(Zaremba et al., 2014)</ref> introduced a dropout (Srivastava, 2013) augmented NNLM. <ref type="bibr" target="#b8">(Gal, 2015;</ref><ref type="bibr" target="#b7">Gal and Ghahramani, 2016)</ref> proposed a new dropout method, which is referred to as Bayesian Dropout below, that improves on the results of <ref type="bibr" target="#b35">(Zaremba et al., 2014)</ref>.</p><p>The skip-gram word2vec model introduced in <ref type="bibr">(Mikolov et al., 2013a;</ref><ref type="bibr">Mikolov et al., 2013b)</ref> learns representations of words. This model learns a representation for each word in its vocabulary, both in an input embedding matrix and in an output embedding matrix. When training is complete, the vectors that are returned are the input embeddings. The output embedding is typically ignored, although <ref type="bibr" target="#b26">(Mitra et al., 2016;</ref><ref type="bibr" target="#b28">Mnih and Kavukcuoglu, 2013)</ref> use both the output and input embeddings of words in order to compute word similarity.</p><p>Recently, <ref type="bibr" target="#b9">(Goldberg and Levy, 2014)</ref> argued that the output embedding of the word2vec skipgram model needs to be different than the input embedding.</p><p>As we show, tying the input and the output embeddings is indeed detrimental in word2vec. However, it improves performance in NNLMs.</p><p>In neural machine translation (NMT) models <ref type="bibr" target="#b17">(Kalchbrenner and Blunsom, 2013;</ref><ref type="bibr" target="#b5">Cho et al., 2014;</ref><ref type="bibr" target="#b34">Sutskever et al., 2014;</ref><ref type="bibr" target="#b1">Bahdanau et al., 2014)</ref>, the decoder, which generates the translation of the input sentence in the target language, is a language model that is conditioned on both the previous words of the output sentence and on the source sentence. State of the art results in NMT have recently been achieved by systems that segment the source and target words into subword units <ref type="bibr">(Sennrich et al., 2016a)</ref>. One such method <ref type="bibr">(Sennrich et al., 2016b)</ref> is based on the byte pair encoding (BPE) compression algorithm <ref type="bibr" target="#b6">(Gage, 1994)</ref>. BPE segments rare words into their more commonly appearing subwords.</p><p>Weight tying was previously used in the log-bilinear model of <ref type="bibr" target="#b27">(Mnih and Hinton, 2009)</ref>, but the decision to use it was not explained, and its effect on the model's performance was not tested. Independently and concurrently with our work <ref type="bibr" target="#b16">(Inan et al., 2016)</ref> presented an explanation for weight tying in NNLMs based on <ref type="bibr" target="#b14">(Hinton et al., 2015)</ref>.</p></div>
<div><head n="3">Weight Tying</head><p>In this work, we employ three different model categories: NNLMs, the word2vec skip-gram model, and NMT models. Weight tying is applied similarly in all models. For translation models, we also present a three-way weight tying method.</p><p>NNLM models contain an input embedding matrix, two LSTM layers (h 1 and h 2 ), a third hidden scores/logits layer h 3 , and a softmax layer. The loss used during training is the cross entropy loss without any regularization terms.</p><p>Following <ref type="bibr" target="#b35">(Zaremba et al., 2014)</ref>, we employ two models: large and small. The large model employs dropout for regularization. The small model is not regularized. Therefore, we propose the following regularization scheme. A projection matrix P ∈ IR H×H is inserted before the output embedding, i.e., h 3 = V P h 2 . The regularizing term λ P 2 is then added to the small model's loss function. In all of our experiments, λ = 0.15.</p><p>Projection regularization allows us to use the same embedding (as both the input/output embedding) with some adaptation that is under regularization. It is, therefore, especially suited for WT.</p><p>While training a vanilla untied NNLM, at timestep t, with current input word sequence i 1:t = [i 1 , ..., i t ] and current target output word o t , the negative log likelihood loss is given by:</p><formula xml:id="formula_0">L t = -log p t (o t |i 1:t ), where p t (o t |i 1:t ) = exp (V ⊤ o t h (t) 2 ) C x=1 exp(V ⊤ x h (t) 2 ) , U k (V k ) is the kth row of U (V ),</formula><p>which corresponds to word k, and h</p><formula xml:id="formula_1">(t)</formula><p>2 is the vector of activations of the topmost LSTM layer's output at time t. For simplicity, we assume that at each timestep t, i t = o t . Optimization of the model is performed using stochastic gradient descent.</p><p>The update for row k of the input embedding is:</p><formula xml:id="formula_2">∂Lt ∂U k = ( C x=1 pt(x|i1:t) • V ⊤ x -V ⊤ ot ) ∂h (t) 2 ∂U i t k = it 0 k = it</formula><p>For the output embedding, row k's update is:</p><formula xml:id="formula_3">∂Lt ∂V k = (pt(ot|i1:t) -1)h (t) 2 k = ot pt(k|i1:t) • h (t) 2 k = ot</formula><p>Therefore, in the untied model, at every timestep, the only row that is updated in the input embedding is the row U it representing the current input word. This means that vectors representing rare words are updated only a small number of times. The output embedding updates every row at each timestep.</p><p>In tied NNLMs, we set U = V = S. The update for each row in S is the sum of the updates obtained for the two roles of S as both an input and output embedding.</p><p>The update for row k = i t is similar to the update of row k in the untied NNLM's output embedding (the only difference being that U and V are both replaced by a single matrix S). In this case, there is no update from the input embedding role of S.</p><p>The update for row k = i t , is made up of a term from the input embedding (case k = i t ) and a term from the output embedding (case k = o t ). The second term grows linearly with p t (i t |i 1:t ), which is expected to be close to zero, since words seldom appear twice in a row (the low probability in the network was also verified experimentally). The update that occurs in this case is, therefore, mostly impacted by the update from the input embedding role of S.</p><p>To conclude, in the tied NNLM, every row of S is updated during each iteration, and for all rows except one, this update is similar to the update of the output embedding of the untied model. This implies a greater degree of similarity of the tied embedding to the untied model's output embedding than to its input embedding.</p><p>The analysis above focuses on NNLMs for brevity. In word2vec, the update rules are similar, just that h (t) 2 is replaced by the identity function. As argued by <ref type="bibr" target="#b9">(Goldberg and Levy, 2014)</ref>, in this case weight tying is not appropriate, because if p t (i t |i 1:t ) is close to zero then so is the norm of the embedding of i t . This argument does not hold for NNLMs, since the LSTM layers cause a decoupling of the input and output embedddings.</p><p>Finally, we evaluate the effect of weight tying in neural translation models. In this model:</p><formula xml:id="formula_4">p t (o t |i 1:t , r) = exp(V ⊤ o t G (t) ) C t x=1 exp(V ⊤ x G (t) )</formula><p>where r = (r 1 , ..., r N ) is the set of words in the source sentence, U and V are the input and output embeddings of the decoder and W is the input embedding of the encoder (in translation models size of the vocabulary of the source / target). G (t)  is the decoder, which receives the context vector, the embedding of the input word (i t ) in U , and its previous state at each timestep. c t is the context vector at timestep t, c t = j∈r a tj h j , where a tj is the weight given to the jth annotation at time t:</p><formula xml:id="formula_5">a tj = exp(e tj )</formula><p>k∈r exp(e ik ) , and e tj = a t (h j ), where a is the alignment model. F is the encoder which produces the sequence of annotations (h 1 , ..., h N ).</p><p>The output of the decoder is then projected to a vector of scores using the output embedding: t) . The scores are then converted to probability values using the softmax function.</p><formula xml:id="formula_6">l t = V G (</formula><p>In our weight tied translation model, we tie the input and output embeddings of the decoder.</p><p>We observed that when preprocessing the ACL WMT 2014 EN→FR<ref type="foot" target="#foot_0">1</ref> and WMT 2015 EN→DE<ref type="foot" target="#foot_1">2</ref> datasets using BPE, many of the subwords appeared in the vocabulary of both the source and the target languages. Tab. 1 shows that up to 90% (85%) of BPE subwords between English and French (German) are shared.</p><p>Based on this observation, we propose threeway weight tying (TWWT), where the input embedding of the decoder, the output embedding of the decoder and the input embedding of the encoder are all tied. The single source/target vocabulary of this model is the union of both the source and target vocabularies. In this model, both in the encoder and decoder, all subwords are embedded in the same duo-lingual space.</p></div>
<div><head n="4">Results</head><p>Our experiments study the quality of various embeddings, the similarity between them, and the impact of tying them on the word2vec skip-gram model, NNLMs, and NMT models.</p></div>
<div><head n="4.1">Quality of Obtained Embeddings</head><p>In order to compare the various embeddings, we pooled five embedding evaluation methods from the literature.</p><p>These evaluation methods involve calculating pairwise (cosine) distances between embeddings and correlating these distances with human judgments of the strength of relationships between concepts. We use: Simlex999 <ref type="bibr" target="#b13">(Hill et al., 2016)</ref>, Verb-143 <ref type="bibr" target="#b2">(Baker et al., 2014)</ref>, MEN <ref type="bibr" target="#b4">(Bruni et al., 2014)</ref>, Rare-Word <ref type="bibr" target="#b18">(Luong et al., 2013)</ref> and MTurk-771 <ref type="bibr" target="#b12">(Halawi et al., 2012)</ref>.</p><p>We begin by training both the tied and untied word2vec models on the text8 3 dataset, using a vocabulary consisting only of words that appear at least five times. As can be seen in Tab. 2, the output embedding is almost as good as the input embedding. As expected, the embedding of the tied model is not competitive. The situation is different when training the small NNLM model on either the Penn Treebank <ref type="bibr" target="#b20">(Marcus et al., 1993)</ref> or text8 datasets (for PTB, we used the same train/validation/test set split and vocabulary as <ref type="bibr" target="#b22">(Mikolov et al., 2011)</ref>, while on text8 we used the split/vocabulary from <ref type="bibr" target="#b25">(Mikolov et al., 2014)</ref>). These results are presented in Tab. 3. In this case, the input embedding is far inferior to the output embedding. The tied embedding is comparable to the output embedding.</p><p>A natural question given these results and the analysis in Sec. 3 is whether the word embedding in the weight tied NNLM model is more similar to the input embedding or to the output embedding between all pairs of words evaluated for the different embeddings: input/output embeddings (of the untied model) and the embeddings of our tied model. We show the results for both the word2vec models and the small and large NNLM models from <ref type="bibr" target="#b35">(Zaremba et al., 2014)</ref>. of the original model. We, therefore, run the following experiment: First, for each embedding, we compute the cosine distances between each pair of words. We then compute Spearman's rank correlation between these vectors of distances. As can be seen in Tab. 4, the results are consistent with our analysis and the results of Tab. 2 and Tab. 3: for word2vec the input and output embeddings are similar to each other and differ from the tied embedding; for the NNLM models, the output embedding and the tied embeddings are similar, the input embedding is somewhat similar to the tied embedding, and differs considerably from the output embedding.</p></div>
<div><head>Model</head></div>
<div><head n="4.2">Neural Network Language Models</head><p>We next study the effect of tying the embeddings on the perplexity obtained by the NNLM models. Following <ref type="bibr" target="#b35">(Zaremba et al., 2014)</ref>, we study two NNLMs. The two models differ mostly in the size of the LSTM layers. In the small model, both LSTM layers contain 200 units and in the large model, both contain 1500 units. In addition, the large model uses three dropout layers, one placed right before the first LSTM layer, one between h 1 and h 2 and one right after h 2 . The dropout probability is 0.65. For both the small and large models, we use the same hyperparameters (i.e. weight initialization, learning rate schedule, batch size) as in <ref type="bibr" target="#b35">(Zaremba et al., 2014)</ref>. 53.5 104.9 100.9</p><p>Table <ref type="table">6</ref>: Word level perplexity on PTB and size for models that do not use dropout.</p><p>The compared models are: KN 5-gram <ref type="bibr" target="#b22">(Mikolov et al., 2011)</ref>, RNN <ref type="bibr" target="#b22">(Mikolov et al., 2011)</ref>, LSTM (Graves, 2013), Stack / Deep RNN <ref type="bibr" target="#b30">(Pascanu et al., 2013)</ref>, FOFE-FNN <ref type="bibr" target="#b37">(Zhang et al., 2015)</ref>, Noisy LSTM <ref type="bibr" target="#b11">(Gülc ¸ehre et al., 2016)</ref>, and the small model from <ref type="bibr" target="#b35">(Zaremba et al., 2014)</ref>. The last three models are our models, which extend the small model. PR -projection regularization. Table <ref type="table">7</ref>: Word level perplexity on the text8, IMDB and BBC datasets. The last three models are our models, which extend the small model (S) of <ref type="bibr" target="#b35">(Zaremba et al., 2014)</ref>.</p></div>
<div><head>Model</head><p>In addition to training our models on PTB and text8, following <ref type="bibr">(Miyamoto and Cho, 2016)</ref>, we also compare the performance of the NNLMs on the BBC <ref type="bibr" target="#b10">(Greene and Cunningham, 2006)</ref> and IMDB <ref type="bibr" target="#b19">(Maas et al., 2011)</ref> datasets, each of which we process and split into a train/validation/test split (we use the same vocabularies as (Miyamoto and Cho, 2016)).</p><p>In the first experiment, which was conducted on the PTB dataset, we compare the perplexity obtained by the large NNLM model and our version in which the input and output embeddings are tied. As can be seen in Tab. 5, weight tying significantly reduces perplexity on both the validation set and the test set, but not on the training set. This indicates less overfitting, as expected due to the reduction in the number of parameters. Recently, <ref type="bibr" target="#b7">(Gal and Ghahramani, 2016)</ref>, proposed a modified model that uses Bayesian dropout and weight decay. They obtained improved performance. When the embeddings of this model are tied, a similar amount of improvement is gained. We tried this with and without weight decay and got similar results in both cases, with slight improvement in the latter model. Finally, by replacing the LSTM with a recurrent highway network <ref type="bibr" target="#b38">(Zilly et al., 2016)</ref>, state of the art results are achieved when applying weight tying. The contribution of WT is also significant in this model. Perplexity results are often reported separately for models with and without dropout. In Tab. 6, we report the results of the small NNLM model, that does not utilize dropout, on PTB. As can be seen, both WT and projection regularization (PR) improve the results. When combining both methods together, state of the art results are obtained. An analog table for text8, IMDB and BBC is Tab. 7, which shows a significant reduction in perplexity across these datasets when both PR and WT are used. PR does not help the large models, which employ dropout for regularization.</p></div>
<div><head n="4.3">Neural Machine Translation</head><p>Finally, we study the impact of weight tying in attention based NMT models, using the DL4MT<ref type="foot" target="#foot_2">4</ref> implementation. We train our EN→FR models on the parallel corpora provided by ACL WMT 2014. We use the data as processed by <ref type="bibr" target="#b5">(Cho et al., 2014)</ref> using the data selection method of <ref type="bibr" target="#b0">(Axelrod et al., 2011)</ref>. For EN→DE we train on data from the translation task of WMT 2015, validate on newstest2013 and test on newstest2014 and newstest2015. Following <ref type="bibr">(Sennrich et al., 2016b)</ref> we learn the BPE segmentation on the union of the vocabularies that we are translating from and to (we use BPE with 89500 merge operations). All models were trained using Adadelta (Zeiler, 2012) for 300K updates, have a hidden layer size of 1000 and all embedding layers are of size 500.</p><p>Tab. 8 shows that even though the weight tied models have about 28% fewer parameters than the baseline models, their performance is similar. This is also the case for the three-way weight tied models, even though they have about 52% fewer parameters than their untied counterparts.</p></div><figure type="table" xml:id="tab_0"><head>Table 1 :</head><label>1</label><figDesc>Shared BPE subwords between pairs of languages.</figDesc><table><row><cell>Language</cell><cell>Subwords</cell><cell>Subwords</cell><cell>Subwords</cell></row><row><cell>pairs</cell><cell cols="2">only in source only in target</cell><cell>in both</cell></row><row><cell>EN→FR</cell><cell>2K</cell><cell>7K</cell><cell>85K</cell></row><row><cell>EN→DE</cell><cell>3K</cell><cell>11K</cell><cell>80K</cell></row></table><note><p>U, V ∈ IR Ct×H and W ∈ IR Cs×H , where C s / C t is the</p></note></figure>
<figure type="table" xml:id="tab_1"><head>Table 3 :</head><label>3</label><figDesc>Comparison of the input/output embeddings of the small model from<ref type="bibr" target="#b35">(Zaremba et al., 2014)</ref> and the embeddings from our weight tied variant. Spearman's correlation ρ is presented.</figDesc><table><row><cell /><cell /><cell cols="4">Input Output Tied</cell></row><row><cell cols="2">Simlex999</cell><cell>0.30</cell><cell>0.29</cell><cell /><cell>0.17</cell></row><row><cell cols="2">Verb-143</cell><cell>0.41</cell><cell>0.34</cell><cell /><cell>0.12</cell></row><row><cell cols="2">MEN</cell><cell>0.66</cell><cell>0.61</cell><cell /><cell>0.50</cell></row><row><cell cols="2">Rare-Word</cell><cell>0.34</cell><cell>0.34</cell><cell /><cell>0.23</cell></row><row><cell cols="2">MTurk-771</cell><cell>0.59</cell><cell>0.54</cell><cell /><cell>0.37</cell></row><row><cell cols="7">Table 2: Comparison of input and output embeddings</cell></row><row><cell cols="7">learned by a word2vec skip-gram model. Results are also</cell></row><row><cell cols="7">shown for the tied word2vec model. Spearman's correlation ρ</cell></row><row><cell cols="7">is reported for five word embedding evaluation benchmarks.</cell></row><row><cell /><cell /><cell>PTB</cell><cell /><cell /><cell>text8</cell></row><row><cell cols="2">Embedding In</cell><cell>Out</cell><cell>Tied</cell><cell>In</cell><cell>Out</cell><cell>Tied</cell></row><row><cell>Simlex999</cell><cell cols="3">0.02 0.13 0.14</cell><cell cols="3">0.17 0.27 0.28</cell></row><row><cell>Verb143</cell><cell cols="3">0.12 0.37 0.32</cell><cell cols="3">0.20 0.35 0.42</cell></row><row><cell>MEN</cell><cell cols="3">0.11 0.21 0.26</cell><cell cols="3">0.26 0.50 0.50</cell></row><row><cell>Rare-Word</cell><cell cols="3">0.28 0.38 0.36</cell><cell cols="3">0.14 0.15 0.17</cell></row><row><cell>MTurk771</cell><cell cols="3">0.17 0.28 0.30</cell><cell cols="3">0.26 0.48 0.45</cell></row></table></figure>
<figure type="table" xml:id="tab_2"><head>Table 4 :</head><label>4</label><figDesc>Spearman's rank correlation ρ of similarity values</figDesc><table><row><cell>3 http://mattmahoney.net/dc/textdata</cell></row></table></figure>
<figure type="table" xml:id="tab_3"><head>Table 5 :</head><label>5</label><figDesc>Word level perplexity (lower is better) on PTB and size (number of parameters) of models that use either dropout (baseline model) or Bayesian dropout (BD). WDweight decay.</figDesc><table><row><cell>Size Train Val. Test</cell></row></table></figure>
<figure type="table" xml:id="tab_6"><head>Table 8 :</head><label>8</label><figDesc>Size (number of parameters) and BLEU score of various translation models. TWWT -three-way weight tying.</figDesc><table><row><cell /><cell>Size</cell><cell>Validation</cell><cell>Test</cell></row><row><cell>EN→FR Baseline</cell><cell>168M</cell><cell>29.49</cell><cell>33.13</cell></row><row><cell cols="2">Decoder WT 122M</cell><cell>29.47</cell><cell>33.26</cell></row><row><cell>TWWT</cell><cell>80M</cell><cell>29.43</cell><cell>33.46</cell></row><row><cell>EN→DE Baseline</cell><cell>165M</cell><cell>20.96</cell><cell>16.79</cell></row><row><cell cols="2">Decoder WT 119M</cell><cell>21.09</cell><cell>16.54</cell></row><row><cell>TWWT</cell><cell>79M</cell><cell>21.02</cell><cell>17.15</cell></row></table></figure>
			<note place="foot" n="1" xml:id="foot_0"><p>http://statmt.org/wmt14/translation-task.html</p></note>
			<note place="foot" n="2" xml:id="foot_1"><p>http://statmt.org/wmt15/translation-task.html</p></note>
			<note place="foot" n="4" xml:id="foot_2"><p>https://github.com/nyu-dl/dl4mt-tutorial</p></note>
		</body>
		</text>
</TEI>