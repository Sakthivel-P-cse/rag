<?xml version='1.0' encoding='utf-8'?>
<TEI xmlns="http://www.tei-c.org/ns/1.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xml:space="preserve" xsi:schemaLocation="http://www.tei-c.org/ns/1.0 https://raw.githubusercontent.com/kermitt2/grobid/master/grobid-home/schemas/xsd/Grobid.xsd">
	<teiHeader xml:lang="en">
		<fileDesc>
			<titleStmt>
				<title level="a" type="main">SEMI-SUPERVISED CLASSIFICATION WITH GRAPH CONVOLUTIONAL NETWORKS</title>
				<funder>
					<orgName type="full">SAP</orgName>
				</funder>
			</titleStmt>
			<publicationStmt>
				<publisher />
				<availability status="unknown"><licence /></availability>
				<date type="published" when="2017-02-22">22 Feb 2017</date>
			</publicationStmt>
			</fileDesc>
		<encodingDesc>
			<appInfo>
				<application version="0.8.0" ident="GROBID" when="2025-12-22T07:54+0000">
					<desc>GROBID - A machine learning software for extracting information from scholarly documents</desc>
					<ref target="https://github.com/kermitt2/grobid" />
				</application>
			</appInfo>
		</encodingDesc>
		<profileDesc>
			<abstract>
<div><p>We present a scalable approach for semi-supervised learning on graph-structured data that is based on an efficient variant of convolutional neural networks which operate directly on graphs. We motivate the choice of our convolutional architecture via a localized first-order approximation of spectral graph convolutions. Our model scales linearly in the number of graph edges and learns hidden layer representations that encode both local graph structure and features of nodes. In a number of experiments on citation networks and on a knowledge graph dataset we demonstrate that our approach outperforms related methods by a significant margin.</p></div>
			</abstract>
		</profileDesc>
	</teiHeader>
	<text xml:lang="en">
		<body>
<div><head n="1">INTRODUCTION</head><p>We consider the problem of classifying nodes (such as documents) in a graph (such as a citation network), where labels are only available for a small subset of nodes. This problem can be framed as graph-based semi-supervised learning, where label information is smoothed over the graph via some form of explicit graph-based regularization <ref type="bibr" target="#b31">(Zhu et al., 2003;</ref><ref type="bibr" target="#b30">Zhou et al., 2004;</ref><ref type="bibr" target="#b2">Belkin et al., 2006;</ref><ref type="bibr" target="#b27">Weston et al., 2012)</ref>, e.g. by using a graph Laplacian regularization term in the loss function:</p><formula xml:id="formula_0">L = L 0 + λL reg , with L reg = i,j A ij f (X i ) -f (X j ) 2 = f (X) ∆f (X) .</formula><p>(1)</p><p>Here, L 0 denotes the supervised loss w.r.t. the labeled part of the graph, f (•) can be a neural networklike differentiable function, λ is a weighing factor and X is a matrix of node feature vectors X i . ∆ = D -A denotes the unnormalized graph Laplacian of an undirected graph G = (V, E) with N nodes v i ∈ V, edges (v i , v j ) ∈ E, an adjacency matrix A ∈ R N ×N (binary or weighted) and a degree matrix D ii = j A ij . The formulation of Eq. 1 relies on the assumption that connected nodes in the graph are likely to share the same label. This assumption, however, might restrict modeling capacity, as graph edges need not necessarily encode node similarity, but could contain additional information.</p><p>In this work, we encode the graph structure directly using a neural network model f (X, A) and train on a supervised target L 0 for all nodes with labels, thereby avoiding explicit graph-based regularization in the loss function. Conditioning f (•) on the adjacency matrix of the graph will allow the model to distribute gradient information from the supervised loss L 0 and will enable it to learn representations of nodes both with and without labels.</p><p>Our contributions are two-fold. Firstly, we introduce a simple and well-behaved layer-wise propagation rule for neural network models which operate directly on graphs and show how it can be motivated from a first-order approximation of spectral graph convolutions <ref type="bibr" target="#b12">(Hammond et al., 2011)</ref>. Secondly, we demonstrate how this form of a graph-based neural network model can be used for fast and scalable semi-supervised classification of nodes in a graph. Experiments on a number of datasets demonstrate that our model compares favorably both in classification accuracy and efficiency (measured in wall-clock time) against state-of-the-art methods for semi-supervised learning.</p></div>
<div><head n="2">FAST APPROXIMATE CONVOLUTIONS ON GRAPHS</head><p>In this section, we provide theoretical motivation for a specific graph-based neural network model f (X, A) that we will use in the rest of this paper. We consider a multi-layer Graph Convolutional Network (GCN) with the following layer-wise propagation rule: l) .</p><formula xml:id="formula_1">H (l+1) = σ D-1 2 Ã D-1 2 H (l) W (</formula><p>(2)</p><p>Here, Ã = A + I N is the adjacency matrix of the undirected graph G with added self-connections. I N is the identity matrix, Dii = j Ãij and W (l) is a layer-specific trainable weight matrix. σ(•)</p><p>denotes an activation function, such as the ReLU(•) = max(0, •). H (l) ∈ R N ×D is the matrix of activations in the l th layer; H (0) = X. In the following, we show that the form of this propagation rule can be motivated<ref type="foot" target="#foot_0">1</ref> via a first-order approximation of localized spectral filters on graphs <ref type="bibr" target="#b12">(Hammond et al., 2011;</ref><ref type="bibr" target="#b6">Defferrard et al., 2016)</ref>.</p></div>
<div><head n="2.1">SPECTRAL GRAPH CONVOLUTIONS</head><p>We consider spectral convolutions on graphs defined as the multiplication of a signal x ∈ R N (a scalar for every node) with a filter g θ = diag(θ) parameterized by θ ∈ R N in the Fourier domain, i.e.:</p><formula xml:id="formula_2">g θ x = U g θ U x ,<label>(3)</label></formula><p>where U is the matrix of eigenvectors of the normalized graph Laplacian L = I N -D -1 2 AD -1 2 = U ΛU , with a diagonal matrix of its eigenvalues Λ and U x being the graph Fourier transform of x. We can understand g θ as a function of the eigenvalues of L, i.e. g θ (Λ). Evaluating Eq. 3 is computationally expensive, as multiplication with the eigenvector matrix U is O(N 2 ). Furthermore, computing the eigendecomposition of L in the first place might be prohibitively expensive for large graphs. To circumvent this problem, it was suggested in <ref type="bibr" target="#b12">Hammond et al. (2011)</ref> that g θ (Λ) can be well-approximated by a truncated expansion in terms of Chebyshev polynomials T k (x) up to K th order:</p><formula xml:id="formula_3">g θ (Λ) ≈ K k=0 θ k T k ( Λ) ,<label>(4)</label></formula><p>with a rescaled Λ = 2 λmax Λ -I N . λ max denotes the largest eigenvalue of L. θ ∈ R K is now a vector of Chebyshev coefficients. The Chebyshev polynomials are recursively defined as</p><formula xml:id="formula_4">T k (x) = 2xT k-1 (x) -T k-2 (x), with T 0 (x) = 1 and T 1 (x) = x.</formula><p>The reader is referred to <ref type="bibr" target="#b12">Hammond et al. (2011)</ref> for an in-depth discussion of this approximation.</p><p>Going back to our definition of a convolution of a signal x with a filter g θ , we now have:</p><formula xml:id="formula_5">g θ x ≈ K k=0 θ k T k ( L)x ,<label>(5)</label></formula><p>with L = 2 λmax L -I N ; as can easily be verified by noticing that (U ΛU ) k = U Λ k U . Note that this expression is now K-localized since it is a K th -order polynomial in the Laplacian, i.e. it depends only on nodes that are at maximum K steps away from the central node (K th -order neighborhood). The complexity of evaluating Eq. 5 is O(|E|), i.e. linear in the number of edges. <ref type="bibr" target="#b6">Defferrard et al. (2016)</ref> use this K-localized convolution to define a convolutional neural network on graphs.</p></div>
<div><head n="2.2">LAYER-WISE LINEAR MODEL</head><p>A neural network model based on graph convolutions can therefore be built by stacking multiple convolutional layers of the form of Eq. 5, each layer followed by a point-wise non-linearity. Now, imagine we limited the layer-wise convolution operation to K = 1 (see Eq. 5), i.e. a function that is linear w.r.t. L and therefore a linear function on the graph Laplacian spectrum.</p><p>In this way, we can still recover a rich class of convolutional filter functions by stacking multiple such layers, but we are not limited to the explicit parameterization given by, e.g., the Chebyshev polynomials. We intuitively expect that such a model can alleviate the problem of overfitting on local neighborhood structures for graphs with very wide node degree distributions, such as social networks, citation networks, knowledge graphs and many other real-world graph datasets. Additionally, for a fixed computational budget, this layer-wise linear formulation allows us to build deeper models, a practice that is known to improve modeling capacity on a number of domains <ref type="bibr" target="#b13">(He et al., 2016)</ref>.</p><p>In this linear formulation of a GCN we further approximate λ max ≈ 2, as we can expect that neural network parameters will adapt to this change in scale during training. Under these approximations Eq. 5 simplifies to:</p><formula xml:id="formula_6">g θ x ≈ θ 0 x + θ 1 (L -I N ) x = θ 0 x -θ 1 D -1 2 AD -1 2 x ,<label>(6)</label></formula><p>with two free parameters θ 0 and θ 1 . The filter parameters can be shared over the whole graph. Successive application of filters of this form then effectively convolve the k th -order neighborhood of a node, where k is the number of successive filtering operations or convolutional layers in the neural network model.</p><p>In practice, it can be beneficial to constrain the number of parameters further to address overfitting and to minimize the number of operations (such as matrix multiplications) per layer. This leaves us with the following expression:</p><formula xml:id="formula_7">g θ x ≈ θ I N + D -1 2 AD -1 2 x ,<label>(7)</label></formula><p>with a single parameter θ = θ 0 = -θ 1 . Note that I N + D -1 2 AD -1 2 now has eigenvalues in the range [0, 2]. Repeated application of this operator can therefore lead to numerical instabilities and exploding/vanishing gradients when used in a deep neural network model. To alleviate this problem, we introduce the following renormalization trick:</p><formula xml:id="formula_8">I N + D -1 2 AD -1 2 → D-1 2 Ã D-1 2 , with Ã = A + I N and Dii = j Ãij .</formula><p>We can generalize this definition to a signal X ∈ R N ×C with C input channels (i.e. a C-dimensional feature vector for every node) and F filters or feature maps as follows:</p><formula xml:id="formula_9">Z = D-1 2 Ã D-1 2 XΘ ,<label>(8)</label></formula><p>where Θ ∈ R C×F is now a matrix of filter parameters and Z ∈ R N ×F is the convolved signal matrix. This filtering operation has complexity O(|E|F C), as ÃX can be efficiently implemented as a product of a sparse matrix with a dense matrix.</p></div>
<div><head n="3">SEMI-SUPERVISED NODE CLASSIFICATION</head><p>Having introduced a simple, yet flexible model f (X, A) for efficient information propagation on graphs, we can return to the problem of semi-supervised node classification. As outlined in the introduction, we can relax certain assumptions typically made in graph-based semi-supervised learning by conditioning our model f (X, A) both on the data X and on the adjacency matrix A of the underlying graph structure. We expect this setting to be especially powerful in scenarios where the adjacency matrix contains information not present in the data X, such as citation links between documents in a citation network or relations in a knowledge graph. The overall model, a multi-layer GCN for semi-supervised learning, is schematically depicted in Figure <ref type="figure" target="#fig_0">1</ref>.</p></div>
<div><head n="3.1">EXAMPLE</head><p>In the following, we consider a two-layer GCN for semi-supervised node classification on a graph with a symmetric adjacency matrix A (binary or weighted). We first calculate Â = D-1 2 Ã D-1 2 in a pre-processing step. Our forward model then takes the simple form: Here, W (0) ∈ R C×H is an input-to-hidden weight matrix for a hidden layer with H feature maps. W (1) ∈ R H×F is a hidden-to-output weight matrix. The softmax activation function, defined as softmax(</p><formula xml:id="formula_10">Z = f (X, A) = softmax Â ReLU ÂXW (0) W (1) . (9) C input layer X 1 X 2 X 3 X 4 F output layer Z 1 Z 2 Z 3 Z 4 hidden layers Y 1 Y 4 1 (a) Graph Convolutional Network</formula><formula xml:id="formula_11">x i ) = 1 Z exp(x i ) with Z = i exp(x i )</formula><p>, is applied row-wise. For semi-supervised multiclass classification, we then evaluate the cross-entropy error over all labeled examples:</p><formula xml:id="formula_12">L = - l∈Y L F f =1 Y lf ln Z lf ,<label>(10)</label></formula><p>where Y L is the set of node indices that have labels.</p><p>The neural network weights W (0) and W (1) are trained using gradient descent. In this work, we perform batch gradient descent using the full dataset for every training iteration, which is a viable option as long as datasets fit in memory. Using a sparse representation for A, memory requirement is O(|E|), i.e. linear in the number of edges. Stochasticity in the training process is introduced via dropout <ref type="bibr" target="#b24">(Srivastava et al., 2014)</ref>. We leave memory-efficient extensions with mini-batch stochastic gradient descent for future work.</p></div>
<div><head n="3.2">IMPLEMENTATION</head><p>In practice, we make use of TensorFlow <ref type="bibr" target="#b0">(Abadi et al., 2015)</ref> for an efficient GPU-based implementation<ref type="foot" target="#foot_1">2</ref> of Eq. 9 using sparse-dense matrix multiplications. The computational complexity of evaluating Eq. 9 is then O(|E|CHF ), i.e. linear in the number of graph edges.</p></div>
<div><head n="4">RELATED WORK</head><p>Our model draws inspiration both from the field of graph-based semi-supervised learning and from recent work on neural networks that operate on graphs. In what follows, we provide a brief overview on related work in both fields.</p></div>
<div><head n="4.1">GRAPH-BASED SEMI-SUPERVISED LEARNING</head><p>A large number of approaches for semi-supervised learning using graph representations have been proposed in recent years, most of which fall into two broad categories: methods that use some form of explicit graph Laplacian regularization and graph embedding-based approaches. Prominent examples for graph Laplacian regularization include label propagation <ref type="bibr" target="#b31">(Zhu et al., 2003)</ref>, manifold regularization <ref type="bibr" target="#b2">(Belkin et al., 2006)</ref> and deep semi-supervised embedding <ref type="bibr" target="#b27">(Weston et al., 2012)</ref>.</p><p>Recently, attention has shifted to models that learn graph embeddings with methods inspired by the skip-gram model <ref type="bibr" target="#b19">(Mikolov et al., 2013)</ref>. DeepWalk <ref type="bibr" target="#b21">(Perozzi et al., 2014)</ref> learns embeddings via the prediction of the local neighborhood of nodes, sampled from random walks on the graph. LINE <ref type="bibr" target="#b25">(Tang et al., 2015)</ref> and node2vec <ref type="bibr" target="#b11">(Grover &amp; Leskovec, 2016)</ref> extend DeepWalk with more sophisticated random walk or breadth-first search schemes. For all these methods, however, a multistep pipeline including random walk generation and semi-supervised training is required where each step has to be optimized separately. Planetoid <ref type="bibr" target="#b28">(Yang et al., 2016)</ref> alleviates this by injecting label information in the process of learning embeddings.</p></div>
<div><head n="4.2">NEURAL NETWORKS ON GRAPHS</head><p>Neural networks that operate on graphs have previously been introduced in </p></div>
<div><head n="5">EXPERIMENTS</head><p>We test our model in a number of experiments: semi-supervised document classification in citation networks, semi-supervised entity classification in a bipartite graph extracted from a knowledge graph, an evaluation of various graph propagation models and a run-time analysis on random graphs.</p></div>
<div><head n="5.1">DATASETS</head><p>We closely follow the experimental setup in <ref type="bibr" target="#b28">Yang et al. (2016)</ref>  <ref type="bibr">et al., 2008)</ref>. The datasets contain sparse bag-of-words feature vectors for each document and a list of citation links between documents. We treat the citation links as (undirected) edges and construct a binary, symmetric adjacency matrix A. Each document has a class label. For training, we only use 20 labels per class, but all feature vectors.</p><p>NELL NELL is a dataset extracted from the knowledge graph introduced in <ref type="bibr" target="#b5">(Carlson et al., 2010)</ref>.</p><p>A knowledge graph is a set of entities connected with directed, labeled edges (relations). We follow the pre-processing scheme as described in <ref type="bibr" target="#b28">Yang et al. (2016)</ref>. We assign separate relation nodes r 1 and r 2 for each entity pair (e 1 , r, e 2 ) as (e 1 , r 1 ) and (e 2 , r 2 ). Entity nodes are described by sparse feature vectors. We extend the number of features in NELL by assigning a unique one-hot representation for every relation node, effectively resulting in a 61,278-dim sparse feature vector per node. The semi-supervised task here considers the extreme case of only a single labeled example per class in the training set. We construct a binary, symmetric adjacency matrix from this graph by setting entries A ij = 1, if one or more edges are present between nodes i and j.</p><p>Random graphs We simulate random graph datasets of various sizes for experiments where we measure training time per epoch. For a dataset with N nodes we create a random graph assigning 2N edges uniformly at random. We take the identity matrix I N as input feature matrix X, thereby implicitly taking a featureless approach where the model is only informed about the identity of each node, specified by a unique one-hot vector. We add dummy labels Y i = 1 for every node.</p></div>
<div><head n="5.2">EXPERIMENTAL SET-UP</head><p>Unless otherwise noted, we train a two-layer GCN as described in Section 3.1 and evaluate prediction accuracy on a test set of 1,000 labeled examples. We provide additional experiments using deeper models with up to 10 layers in Appendix B. We choose the same dataset splits as in Yang et al. ( <ref type="formula">2016</ref>) with an additional validation set of 500 labeled examples for hyperparameter optimization (dropout rate for all layers, L2 regularization factor for the first GCN layer and number of hidden units). We do not use the validation set labels for training.</p><p>For the citation network datasets, we optimize hyperparameters on Cora only and use the same set of parameters for Citeseer and Pubmed. We train all models for a maximum of 200 epochs (training iterations) using Adam (Kingma &amp; Ba, 2015) with a learning rate of 0.01 and early stopping with a window size of 10, i.e. we stop training if the validation loss does not decrease for 10 consecutive epochs. We initialize weights using the initialization described in <ref type="bibr" target="#b9">Glorot &amp; Bengio (2010)</ref> and accordingly (row-)normalize input feature vectors. On the random graph datasets, we use a hidden layer size of 32 units and omit regularization (i.e. neither dropout nor L2 regularization).</p></div>
<div><head n="5.3">BASELINES</head><p>We compare against the same baseline methods as in <ref type="bibr" target="#b28">Yang et al. (2016)</ref>, i.e. label propagation (LP) <ref type="bibr" target="#b31">(Zhu et al., 2003)</ref>, semi-supervised embedding (SemiEmb) <ref type="bibr" target="#b27">(Weston et al., 2012)</ref>, manifold regularization (ManiReg) <ref type="bibr" target="#b2">(Belkin et al., 2006)</ref> and skip-gram based graph embeddings (DeepWalk) <ref type="bibr" target="#b21">(Perozzi et al., 2014)</ref>. We omit TSVM <ref type="bibr" target="#b14">(Joachims, 1999)</ref>, as it does not scale to the large number of classes in one of our datasets.</p><p>We further compare against the iterative classification algorithm (ICA) proposed in <ref type="bibr" target="#b17">Lu &amp; Getoor (2003)</ref> in conjunction with two logistic regression classifiers, one for local node features alone and one for relational classification using local features and an aggregation operator as described in <ref type="bibr" target="#b23">Sen et al. (2008)</ref>. We first train the local classifier using all labeled training set nodes and use it to bootstrap class labels of unlabeled nodes for relational classifier training. We run iterative classification (relational classifier) with a random node ordering for 10 iterations on all unlabeled nodes (bootstrapped using the local classifier). L2 regularization parameter and aggregation operator (count vs. prop, see <ref type="bibr" target="#b23">Sen et al. (2008)</ref>) are chosen based on validation set performance for each dataset separately.</p><p>Lastly, we compare against Planetoid <ref type="bibr" target="#b28">(Yang et al., 2016)</ref>, where we always choose their bestperforming model variant (transductive vs. inductive) as a baseline.</p></div>
<div><head n="6">RESULTS</head></div>
<div><head n="6.1">SEMI-SUPERVISED NODE CLASSIFICATION</head><p>Results are summarized in Table <ref type="table" target="#tab_2">2</ref>. Reported numbers denote classification accuracy in percent. For ICA, we report the mean accuracy of 100 runs with random node orderings. Results for all other baseline methods are taken from the Planetoid paper <ref type="bibr" target="#b28">(Yang et al., 2016)</ref>. Planetoid* denotes the best model for the respective dataset out of the variants presented in their paper. GCN (rand. splits) 67.9 ± 0.5 80.1 ± 0.5 78.9 ± 0.7 58.4 ± 1.7</p><p>We further report wall-clock training time in seconds until convergence (in brackets) for our method (incl. evaluation of validation error) and for Planetoid. For the latter, we used an implementation provided by the authors<ref type="foot" target="#foot_2">3</ref> and trained on the same hardware (with GPU) as our GCN model. We trained and tested our model on the same dataset splits as in <ref type="bibr" target="#b28">Yang et al. (2016)</ref> and report mean accuracy of 100 runs with random weight initializations. We used the following sets of hyperparameters for Citeseer, Cora and Pubmed: 0.5 (dropout rate), 5 • 10 -4 (L2 regularization) and 16 (number of hidden units); and for NELL: 0.1 (dropout rate), 1 • 10 -5 (L2 regularization) and 64 (number of hidden units).</p><p>In addition, we report performance of our model on 10 randomly drawn dataset splits of the same size as in <ref type="bibr" target="#b28">Yang et al. (2016)</ref>, denoted by GCN (rand. splits). Here, we report mean and standard error of prediction accuracy on the test set split in percent.</p></div>
<div><head n="6.2">EVALUATION OF PROPAGATION MODEL</head><p>We compare different variants of our proposed per-layer propagation model on the citation network datasets. We follow the experimental set-up described in the previous section. Results are summarized in Table <ref type="table" target="#tab_3">3</ref>. The propagation model of our original GCN model is denoted by renormalization trick (in bold). In all other cases, the propagation model of both neural network layers is replaced with the model specified under propagation model. Reported numbers denote mean classification accuracy for 100 repeated runs with random weight matrix initializations. In case of multiple variables Θ i per layer, we impose L2 regularization on all weight matrices of the first layer.  Here, we report results for the mean training time per epoch (forward pass, cross-entropy calculation, backward pass) for 100 epochs on simulated random graphs, measured in seconds wall-clock time. See Section 5.1 for a detailed description of the random graph dataset used in these experiments. We compare results on a GPU and on a CPU-only implementation<ref type="foot" target="#foot_3">4</ref> in TensorFlow <ref type="bibr" target="#b0">(Abadi et al., 2015)</ref>. Figure <ref type="figure" target="#fig_1">2</ref> summarizes the results.</p></div>
<div><head n="7">DISCUSSION</head></div>
<div><head n="7.1">SEMI-SUPERVISED MODEL</head><p>In the experiments demonstrated here, our method for semi-supervised node classification outperforms recent related methods by a significant margin. Methods based on graph-Laplacian regularization <ref type="bibr" target="#b31">(Zhu et al., 2003;</ref><ref type="bibr" target="#b2">Belkin et al., 2006;</ref><ref type="bibr" target="#b27">Weston et al., 2012)</ref> are most likely limited due to their assumption that edges encode mere similarity of nodes. Skip-gram based methods on the other hand are limited by the fact that they are based on a multi-step pipeline which is difficult to optimize. Our proposed model can overcome both limitations, while still comparing favorably in terms of efficiency (measured in wall-clock time) to related methods. Propagation of feature information from neighboring nodes in every layer improves classification performance in comparison to methods like ICA <ref type="bibr" target="#b17">(Lu &amp; Getoor, 2003)</ref>, where only label information is aggregated.</p><p>We have further demonstrated that the proposed renormalized propagation model (Eq. 8) offers both improved efficiency (fewer parameters and operations, such as multiplication or addition) and better predictive performance on a number of datasets compared to a naïve 1 st -order model (Eq. 6) or higher-order graph convolutional models using Chebyshev polynomials (Eq. 5).</p></div>
<div><head n="7.2">LIMITATIONS AND FUTURE WORK</head><p>Here, we describe several limitations of our current model and outline how these might be overcome in future work.</p></div>
<div><head>Memory requirement</head><p>In the current setup with full-batch gradient descent, memory requirement grows linearly in the size of the dataset. We have shown that for large graphs that do not fit in GPU memory, training on CPU can still be a viable option. Mini-batch stochastic gradient descent can alleviate this issue. The procedure of generating mini-batches, however, should take into account the number of layers in the GCN model, as the K th -order neighborhood for a GCN with K layers has to be stored in memory for an exact procedure. For very large and densely connected graph datasets, further approximations might be necessary.</p><p>Directed edges and edge features Our framework currently does not naturally support edge features and is limited to undirected graphs (weighted or unweighted). Results on NELL however show that it is possible to handle both directed edges and edge features by representing the original directed graph as an undirected bipartite graph with additional nodes that represent edges in the original graph (see Section 5.1 for details).</p><p>Limiting assumptions Through the approximations introduced in Section 2, we implicitly assume locality (dependence on the K th -order neighborhood for a GCN with K layers) and equal importance of self-connections vs. edges to neighboring nodes. For some datasets, however, it might be beneficial to introduce a trade-off parameter λ in the definition of Ã:</p><formula xml:id="formula_13">Ã = A + λI N .<label>(11)</label></formula><p>This parameter now plays a similar role as the trade-off parameter between supervised and unsupervised loss in the typical semi-supervised setting (see Eq. 1). Here, however, it can be learned via gradient descent.</p></div>
<div><head n="8">CONCLUSION</head><p>We have introduced a novel approach for semi-supervised classification on graph-structured data.</p><p>Our GCN model uses an efficient layer-wise propagation rule that is based on a first-order approximation of spectral convolutions on graphs. Experiments on a number of network datasets suggest that the proposed GCN model is capable of encoding both graph structure and node features in a way useful for semi-supervised classification. In this setting, our model outperforms several recently proposed methods by a significant margin, while being computationally efficient.</p></div>
<div><head>A RELATION TO WEISFEILER-LEHMAN ALGORITHM</head><p>A neural network model for graph-structured data should ideally be able to learn representations of nodes in a graph, taking both the graph structure and feature description of nodes into account. A well-studied framework for the unique assignment of node labels given a graph and (optionally) discrete initial node labels is provided by the 1-dim Weisfeiler-Lehman (WL-1) algorithm <ref type="bibr" target="#b26">(Weisfeiler &amp; Lehmann, 1968)</ref>:</p><p>Algorithm 1: WL-1 algorithm <ref type="bibr" target="#b26">(Weisfeiler &amp; Lehmann, 1968)</ref> Input: Initial node coloring (h</p><formula xml:id="formula_14">(0) 1 , h<label>(0)</label></formula><p>2 , ..., h</p><p>N ) Output: Final node coloring (h</p><formula xml:id="formula_16">(T ) 1 , h (T ) 2 , ..., h (T ) N ) t ← 0; repeat for v i ∈ V do h (t+1) i ← hash j∈Ni h (t) j ; t ← t + 1; until stable node coloring is reached;</formula><p>Here, h (t) i denotes the coloring (label assignment) of node v i (at iteration t) and N i is its set of neighboring node indices (irrespective of whether the graph includes self-connections for every node or not). hash(•) is a hash function. For an in-depth mathematical discussion of the WL-1 algorithm see, e.g., <ref type="bibr" target="#b7">Douglas (2011)</ref>.</p><p>We can replace the hash function in Algorithm 1 with a neural network layer-like differentiable function with trainable parameters as follows:</p><formula xml:id="formula_17">h (l+1) i = σ   j∈Ni 1 c ij h (l) j W (l)   ,<label>(12)</label></formula><p>where c ij is an appropriately chosen normalization constant for the edge (v i , v j ). Further, we can take h</p><p>(l)</p><p>i now to be a vector of activations of node i in the l th neural network layer. W (l) is a layer-specific weight matrix and σ(•) denotes a differentiable, non-linear activation function.</p><p>By choosing c ij = d i d j , where d i = |N i | denotes the degree of node v i , we recover the propagation rule of our Graph Convolutional Network (GCN) model in vector form (see Eq. 2)<ref type="foot" target="#foot_4">5</ref> . This-loosely speaking-allows us to interpret our GCN model as a differentiable and parameterized generalization of the 1-dim Weisfeiler-Lehman algorithm on graphs.</p></div>
<div><head>A.1 NODE EMBEDDINGS WITH RANDOM WEIGHTS</head><p>From the analogy with the Weisfeiler-Lehman algorithm, we can understand that even an untrained GCN model with random weights can serve as a powerful feature extractor for nodes in a graph. As an example, consider the following 3-layer GCN model:</p><formula xml:id="formula_18">Z = tanh Â tanh Â tanh ÂXW (0) W (1) W (2) ,<label>(13)</label></formula><p>with weight matrices W (l) initialized at random using the initialization described in <ref type="bibr" target="#b9">Glorot &amp; Bengio (2010)</ref>. Â, X and Z are defined as in Section 3.1.</p><p>We apply this model on Zachary's karate club network <ref type="bibr" target="#b29">(Zachary, 1977)</ref>. This graph contains 34 nodes, connected by 154 (undirected and unweighted) edges. Every node is labeled by one of four classes, obtained via modularity-based clustering <ref type="bibr" target="#b3">(Brandes et al., 2008)</ref>. See Figure <ref type="figure" target="#fig_2">3a</ref> for an illustration.  <ref type="bibr" target="#b29">(Zachary, 1977)</ref>, colors denote communities obtained via modularity-based clustering <ref type="bibr" target="#b3">(Brandes et al., 2008)</ref>. Right: Embeddings obtained from an untrained 3-layer GCN model (Eq. 13) with random weights applied to the karate club network. Best viewed on a computer screen.</p><p>We take a featureless approach by setting X = I N , where I N is the N by N identity matrix. N is the number of nodes in the graph. Note that nodes are randomly ordered (i.e. ordering contains no information). Furthermore, we choose a hidden layer dimensionality<ref type="foot" target="#foot_5">6</ref> of 4 and a two-dimensional output (so that the output can immediately be visualized in a 2-dim plot).</p><p>Figure <ref type="figure" target="#fig_2">3b</ref> shows a representative example of node embeddings (outputs Z) obtained from an untrained GCN model applied to the karate club network. These results are comparable to embeddings obtained from DeepWalk <ref type="bibr" target="#b21">(Perozzi et al., 2014)</ref>, which uses a more expensive unsupervised training procedure.</p><p>A.2 SEMI-SUPERVISED NODE EMBEDDINGS On this simple example of a GCN applied to the karate club network it is interesting to observe how embeddings react during training on a semi-supervised classification task. Such a visualization (see Figure <ref type="figure">4</ref>) provides insights into how the GCN model can make use of the graph structure (and of features extracted from the graph structure at later layers) to learn embeddings that are useful for a classification task.</p><p>We consider the following semi-supervised learning setup: we add a softmax layer on top of our model (Eq. 13) and train using only a single labeled example per class (i.e. a total number of 4 labeled nodes). We train for 300 training iterations using Adam (Kingma &amp; Ba, 2015) with a learning rate of 0.01 on a cross-entropy loss. </p></div>
<div><head>B EXPERIMENTS ON MODEL DEPTH</head><p>In these experiments, we investigate the influence of model depth (number of layers) on classification performance. We report results on a 5-fold cross-validation experiment on the Cora, Citeseer and Pubmed datasets <ref type="bibr" target="#b23">(Sen et al., 2008</ref>) using all labels. In addition to the standard GCN model (Eq. 2), we report results on a model variant where we use residual connections <ref type="bibr" target="#b13">(He et al., 2016)</ref> between hidden layers to facilitate training of deeper models by enabling the model to carry over information from the previous layer's input:</p><formula xml:id="formula_19">H (l+1) = σ D-1 2 Ã D-1 2 H (l) W (l) + H (l) . (<label>14</label></formula><formula xml:id="formula_20">)</formula><p>On each cross-validation split, we train for 400 epochs (without early stopping) using the Adam optimizer <ref type="bibr" target="#b15">(Kingma &amp; Ba, 2015)</ref> with a learning rate of 0.01. Other hyperparameters are chosen as follows: 0.5 (dropout rate, first and last layer), 5 • 10 -4 (L2 regularization, first layer), 16 (number of units for each hidden layer) and 0.01 (learning rate). Results are summarized in Figure <ref type="figure" target="#fig_4">5</ref>. We show results both for a standard GCN model (dashed lines) and a model with added residual connections <ref type="bibr" target="#b13">(He et al., 2016)</ref> between hidden layers (solid lines).</p><p>For the datasets considered here, best results are obtained with a 2-or 3-layer model. We observe that for models deeper than 7 layers, training without the use of residual connections can become difficult, as the effective context size for each node increases by the size of its K th -order neighborhood (for a model with K layers) with each additional layer. Furthermore, overfitting can become an issue as the number of parameters increases with model depth.</p></div><figure xml:id="fig_0"><head>Figure 1 :</head><label>1</label><figDesc>Figure 1: Left: Schematic depiction of multi-layer Graph Convolutional Network (GCN) for semisupervised learning with C input channels and F feature maps in the output layer. The graph structure (edges shown as black lines) is shared over layers, labels are denoted by Y i . Right: t-SNE (Maaten &amp; Hinton, 2008) visualization of hidden layer activations of a two-layer GCN trained on the Cora dataset (Sen et al., 2008) using 5% of labels. Colors denote document class.</figDesc></figure>
<figure xml:id="fig_1"><head>Figure 2 :</head><label>2</label><figDesc>Figure 2: Wall-clock time per epoch for random graphs. (*) indicates out-of-memory error.</figDesc></figure>
<figure xml:id="fig_2"><head>Figure 3 :</head><label>3</label><figDesc>Figure3: Left: Zachary's karate club network<ref type="bibr" target="#b29">(Zachary, 1977)</ref>, colors denote communities obtained via modularity-based clustering<ref type="bibr" target="#b3">(Brandes et al., 2008)</ref>. Right: Embeddings obtained from an untrained 3-layer GCN model (Eq. 13) with random weights applied to the karate club network. Best viewed on a computer screen.</figDesc></figure>
<figure xml:id="fig_3"><head>Figure 4 Figure 4 :</head><label>44</label><figDesc>Figure4shows the evolution of node embeddings over a number of training iterations. The model succeeds in linearly separating the communities based on minimal supervision and the graph structure alone. A video of the full training process can be found on our website 7 .</figDesc></figure>
<figure xml:id="fig_4"><head>Figure 5 :</head><label>5</label><figDesc>Figure5: Influence of model depth (number of layers) on classification performance. Markers denote mean classification accuracy (training vs. testing) for 5-fold cross-validation. Shaded areas denote standard error. We show results both for a standard GCN model (dashed lines) and a model with added residual connections<ref type="bibr" target="#b13">(He et al., 2016)</ref> between hidden layers (solid lines).</figDesc></figure>
<figure type="table" xml:id="tab_0"><head /><label /><figDesc><ref type="bibr" target="#b10">Gori et al. (2005)</ref>;<ref type="bibr" target="#b22">Scarselli et al. (2009)</ref> as a form of recurrent neural network. Their framework requires the repeated application of contraction maps as propagation functions until node representations reach a stable fixed point. This restriction was later alleviated in<ref type="bibr" target="#b16">Li et al. (2016)</ref> by introducing modern practices for recurrent neural network training to the original graph neural network framework.<ref type="bibr" target="#b8">Duvenaud et al. (2015)</ref> introduced a convolution-like propagation rule on graphs and methods for graph-level classification. Their approach requires to learn node degree-specific weight matrices which does not scale to large graphs with wide node degree distributions. Our model instead uses a single weight matrix per layer and deals with varying node degrees through an appropriate normalization of the adjacency matrix (see Section 3.1).</figDesc><table /><note><p><p><p><p><p><p><p><p><p><p><p><p><p><p>A related approach to node classification with a graph-based neural network was recently introduced in</p><ref type="bibr" target="#b1">Atwood &amp; Towsley (2016)</ref></p>. They report O(N 2 ) complexity, limiting the range of possible applications. In a different yet related model,</p><ref type="bibr" target="#b20">Niepert et al. (2016)</ref> </p>convert graphs locally into sequences that are fed into a conventional 1D convolutional neural network, which requires the definition of a node ordering in a pre-processing step.</p>Our method is based on spectral graph convolutional neural networks, introduced in Bruna et al. (</p>2014</p>) and later extended by</p><ref type="bibr" target="#b6">Defferrard et al. (2016)</ref> </p>with fast localized convolutions. In contrast to these works, we consider here the task of transductive node classification within networks of significantly larger scale. We show that in this setting, a number of simplifications (see Section 2.2) can be introduced to the original frameworks of</p><ref type="bibr" target="#b4">Bruna et al. (2014)</ref> </p>and</p><ref type="bibr" target="#b6">Defferrard et al. (2016)</ref> </p>that improve scalability and classification performance in large-scale networks.</p></note></figure>
<figure type="table" xml:id="tab_1"><head>Table 1 :</head><label>1</label><figDesc>. Dataset statistics are summarized in Table1. In the citation network datasets-Citeseer, Cora and Pubmed<ref type="bibr" target="#b23">(Sen et al., 2008)</ref>-nodes are documents and edges are citation links. Label rate denotes the number of labeled nodes that are used for training divided by the total number of nodes in each dataset. NELL<ref type="bibr" target="#b5">(Carlson et al., 2010;</ref><ref type="bibr" target="#b28">Yang et al., 2016</ref>) is a bipartite graph dataset extracted from a knowledge graph with 55,864 relation nodes and 9,891 entity nodes. Dataset statistics, as reported in<ref type="bibr" target="#b28">Yang et al. (2016)</ref>.Citation networks We consider three citation network datasets: Citeseer, Cora and Pubmed(Sen   </figDesc><table><row><cell>Dataset</cell><cell>Type</cell><cell>Nodes</cell><cell cols="4">Edges Classes Features Label rate</cell></row><row><cell cols="2">Citeseer Citation network</cell><cell>3,327</cell><cell>4,732</cell><cell>6</cell><cell>3,703</cell><cell>0.036</cell></row><row><cell>Cora</cell><cell>Citation network</cell><cell>2,708</cell><cell>5,429</cell><cell>7</cell><cell>1,433</cell><cell>0.052</cell></row><row><cell cols="3">Pubmed Citation network 19,717</cell><cell>44,338</cell><cell>3</cell><cell>500</cell><cell>0.003</cell></row><row><cell>NELL</cell><cell cols="3">Knowledge graph 65,755 266,144</cell><cell>210</cell><cell>5,414</cell><cell>0.001</cell></row></table></figure>
<figure type="table" xml:id="tab_2"><head>Table 2 :</head><label>2</label><figDesc>Summary of results in terms of classification accuracy (in percent).</figDesc><table><row><cell>Method</cell><cell>Citeseer</cell><cell>Cora</cell><cell>Pubmed</cell><cell>NELL</cell></row><row><cell>ManiReg [3]</cell><cell>60.1</cell><cell>59.5</cell><cell>70.7</cell><cell>21.8</cell></row><row><cell>SemiEmb [28]</cell><cell>59.6</cell><cell>59.0</cell><cell>71.1</cell><cell>26.7</cell></row><row><cell>LP [32]</cell><cell>45.3</cell><cell>68.0</cell><cell>63.0</cell><cell>26.5</cell></row><row><cell>DeepWalk [22]</cell><cell>43.2</cell><cell>67.2</cell><cell>65.3</cell><cell>58.1</cell></row><row><cell>ICA [18]</cell><cell>69.1</cell><cell>75.1</cell><cell>73.9</cell><cell>23.1</cell></row><row><cell>Planetoid* [29]</cell><cell cols="4">64.7 (26s) 75.7 (13s) 77.2 (25s) 61.9 (185s)</cell></row><row><cell>GCN (this paper)</cell><cell>70.3 (7s)</cell><cell>81.5 (4s)</cell><cell cols="2">79.0 (38s) 66.0 (48s)</cell></row></table></figure>
<figure type="table" xml:id="tab_3"><head>Table 3 :</head><label>3</label><figDesc>Comparison of propagation models.</figDesc><table><row><cell>Description</cell><cell /><cell>Propagation model</cell><cell cols="3">Citeseer Cora Pubmed</cell></row><row><cell>Chebyshev filter (Eq. 5)</cell><cell>K = 3 K = 2</cell><cell>K k=0 T k ( L)XΘ k</cell><cell>69.8 69.6</cell><cell>79.5 81.2</cell><cell>74.4 73.8</cell></row><row><cell cols="2">1 st -order model (Eq. 6) Single parameter (Eq. 7) Renormalization trick (Eq. 8)</cell><cell>XΘ 0 + D -1 2 AD -1 2 XΘ 1 2 AD -1 2 )XΘ (I N + D -1 D-1 2 Ã D-1 2 XΘ</cell><cell cols="2">68.3 69.3 70.3 81.5 80.0 79.2</cell><cell>77.5 77.4 79.0</cell></row><row><cell>1 st -order term only</cell><cell /><cell>D -1 2 AD -1 2 XΘ</cell><cell>68.7</cell><cell>80.5</cell><cell>77.8</cell></row><row><cell>Multi-layer perceptron</cell><cell /><cell>XΘ</cell><cell>46.5</cell><cell>55.1</cell><cell>71.4</cell></row></table></figure>
			<note place="foot" n="1" xml:id="foot_0"><p>We provide an alternative interpretation of this propagation rule based on the Weisfeiler-Lehman algorithm<ref type="bibr" target="#b26">(Weisfeiler &amp; Lehmann, 1968</ref>) in Appendix A.</p></note>
			<note place="foot" n="2" xml:id="foot_1"><p>Code to reproduce our experiments is available at https://github.com/tkipf/gcn.</p></note>
			<note place="foot" n="3" xml:id="foot_2"><p>https://github.com/kimiyoung/planetoid</p></note>
			<note place="foot" n="4" xml:id="foot_3"><p>Hardware used: 16-core Intel R Xeon R CPU E5-2640 v3 @ 2.60GHz, GeForce R GTX TITAN X</p></note>
			<note place="foot" n="5" xml:id="foot_4"><p>Note that we here implicitly assume that self-connections have already been added to every node in the graph (for a clutter-free notation).</p></note>
			<note place="foot" n="6" xml:id="foot_5"><p>We originally experimented with a hidden layer dimensionality of 2 (i.e. same as output layer), but observed that a dimensionality of 4 resulted in less frequent saturation of tanh(•) units and therefore visually more pleasing results.</p></note>
			<note place="foot" n="7" xml:id="foot_6"><p>http://tkipf.github.io/graph-convolutional-networks/</p></note>
		</body>
		</text>
</TEI>